# 테스트 실행 규칙

이 저장소의 테스트 분류와 실행 기준은 [Docs/TESTING.md](Docs/TESTING.md)를 따른다.

- 테스트는 `Tests/<Hub|NaiTail|AnimaTail|GalleryTail|CensorTail>/<development|release>/`에 둔다.
- 일반 개발에서는 수정한 기능 및 영향을 받는 코어·연동 테스트만 실행한다. 관련 코드나 테스트,
  의존성, 실행 환경이 바뀌지 않은 기존 통과 항목은 재실행하지 않는다.
- 시작할 때 이번 작업에서 바꾼 파일을 기준으로 범위를 정한다. 이전 작업에서 남은 변경 파일을
  이번 작업의 검증 범위에 무조건 포함하지 않는다. `--files` 또는 `--suite`로 범위를 지정한다.
- `npm test`와 `npm run check`는 개발 테스트만 실행한다. 전 범위 실행은 명시적으로 전 범위
  회귀검증을 요청받았거나 공용 계약 변경이 전 범위에 영향을 줄 때만 사용한다.
- 릴리즈 테스트는 사용자가 배포 또는 릴리즈 준비를 요청한 경우에만 실행한다.
  그때 `npm run test:release -- --release-prep --scope <대상>`을 사용한다.
- 개발 중 배포 폴더 청결도, 전체 런타임·모델 구비, 버전 정합성, 포터블 복사 기동 등을
  완료 조건으로 요구하지 않는다. 해당 로직 자체를 수정하면 관련 개발 테스트를 실행한다.
- 실행 결과는 이번 실행 통과, 기존 통과 기록 재사용, 영향 범위 밖 스킵, 릴리즈 단계 스킵,
  실패·환경 차단을 구분해서 보고한다. 스킵을 통과로 표현하지 않는다.
- 외부 API 생성, GPU 실생성, 설치·다운로드는 자동 단위 테스트에 포함하지 않는다.
  변경 기능에 필요한 경우에만 기존 사용자 승인 범위 안에서 별도 검증한다.
